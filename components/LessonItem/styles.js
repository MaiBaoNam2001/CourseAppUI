import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        marginVertical: 5,
    },
    content: {
        marginLeft: 10
    },
    subject: {
        fontWeight: 'bold',
    },
    createdDate: {
        fontSize: 12,
    }
});

export default Styles;